apiVersion: v1
kind: Pod
metadata:
  namespace: dev
  labels: 
    run: backend-pod
  name: backend-pod
spec:
  nodeSelector:
    ssd: "true"
  containers:
  - image: centos
    name: centos
    stdin: true
    tty: true

