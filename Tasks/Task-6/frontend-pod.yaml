apiVersion: v1
kind: Pod
metadata:
  labels:
    run: frontend-pod
  name: frontend-pod
spec:
  nodeSelector:
    ssd: "true"
  containers:
  - image: centos
    name: centos
    stdin: true
    tty: true

